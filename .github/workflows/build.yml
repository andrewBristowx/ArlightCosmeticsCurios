name: Importar fuente y compilar

on:
  workflow_dispatch:
  push:
    paths:
      - '.github/workflows/build.yml'

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      - name: Descargar repositorio
        uses: actions/checkout@v4

      - name: Configurar Java 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - name: Descargar fuente estable 1.11.0
        shell: bash
        run: |
          set -euo pipefail
          FILE_ID='186ojyjbYxnKBvF98Pg8hlk9-e3HYV8mB'
          curl -L --fail --retry 3 \
            "https://drive.usercontent.google.com/download?id=${FILE_ID}&export=download&confirm=t" \
            -o /tmp/source.zip
          unzip -t /tmp/source.zip
          rm -rf /tmp/source-extracted
          mkdir -p /tmp/source-extracted
          unzip -q /tmp/source.zip -d /tmp/source-extracted

      - name: Importar fuente al repositorio
        shell: bash
        run: |
          set -euo pipefail
          ROOT_FILE="$(find /tmp/source-extracted -maxdepth 5 -type f \( -name gradlew -o -name build.gradle -o -name build.gradle.kts \) | head -n 1)"
          if [ -z "$ROOT_FILE" ]; then
            echo 'No se encontró un proyecto Gradle en el ZIP.' >&2
            exit 1
          fi
          ROOT="$(dirname "$ROOT_FILE")"
          rsync -a --delete --exclude '.git/' --exclude '.github/' "$ROOT/" ./
          printf '%s\n' 'ArlightCosmeticsCurios 1.11.0 COMPANION-MODEL-OVERHAUL' > SOURCE_VERSION.txt
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          if ! git diff --cached --quiet; then
            git commit -m 'Importar ArlightCosmeticsCurios 1.11.0'
            git push
          fi

      - name: Compilar con Gradle
        shell: bash
        run: |
          set -euo pipefail
          chmod +x ./gradlew
          ./gradlew --no-daemon clean build

      - name: Reunir JAR
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p dist
          find build/libs -maxdepth 1 -type f -name '*.jar' \
            ! -name '*-sources.jar' ! -name '*-javadoc.jar' \
            -exec cp {} dist/ \;
          test -n "$(find dist -maxdepth 1 -type f -name '*.jar' -print -quit)"
          ls -lh dist

      - name: Subir JAR compilado
        uses: actions/upload-artifact@v4
        with:
          name: ArlightCosmeticsCurios-1.11.0-JAR
          path: dist/*.jar
          if-no-files-found: error
